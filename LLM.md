```python

Base LLM:
  - purpose:predicts next word based on text training data
  
  - Given that "once upon a time, there was a unicorn", predicting "that lived in a magical forest with all her unicorn friends"
  
  - similar to pre-training stage


Instruction Tuned LLM:
  - purpose: tries to follow instructions
  
  - based on the LLM and fine-tuning both inputs and outputs(training based on both inputs and outputs
    - refined using reinforcement learning
  
  - What is the capital of France? The capital of France is Paris.






```
